# 드라이브 → 코드 → 배포 파이프라인 (새 세션 필독)

이 문서는 **다른 Claude 세션이 이 프로젝트를 이어받아 "드라이브 확인 → 데이터 반영 →
배포"를 똑같이 할 수 있도록** 만든 절차서다. `HANDOFF.md`가 "무엇이 어떻게 바뀌었는지"의
기록이라면, 이 문서는 "그 반영 작업을 실제로 어떻게 하는지"의 절차다. 순서대로 읽어라.

이 문서는 2026-09-22에, 두 개의 클론과 두 개의 인수인계 문서가 서로 모르는 채
같은 저장소에 동시에 작업해서 생긴 혼란을 겪은 뒤 만들어졌다. 아래 0장이 그 사고 얘기다 —
왜 이 규칙들이 있는지 이해하려면 먼저 읽을 것.

---

## 0. 작업 디렉토리는 하나뿐이다 — 확인부터

**정식 작업 디렉토리는 `/Users/gilbert/japantrip` 하나뿐이다.**

2026-09-22에 `/Users/gilbert/Downloads/japantrip`라는 별도 클론이 발견됐다. 거기엔
`CLAUDE.md`(개념도·KML 파일 등 이미 삭제된 옛날 기능을 전제로 한 완전히 낡은 버전)와
`HANDOFF-2.md`(그 클론에서 진행된 별도 세션의 작업 기록)가 있었다. 그 세션은 실제
사실 오류 여러 건을 실측으로 찾아 고쳤지만(원더크루즈 요일별 운항, 돌고래 공연 시각,
고베 환승역, 스마 시월드 요금 등 — 전부 `/Users/gilbert/japantrip`에 커밋됨), **다른 클론에서
작업한다는 것 자체가 다음 문제를 만든다**:
- 어느 쪽 `CLAUDE.md`/`HANDOFF.md`가 최신인지 헷갈림 (Downloads 쪽은 이미 몇 세대 뒤처져 있었음)
- 두 클론이 같은 origin에 push하면 커밋 히스토리는 하나로 합쳐지지만, **각 클론에 남는
  로컬 문서(HANDOFF-2.md 같은)는 git에 안 올라가서 다른 쪽에서 안 보임**
- "유모차 없음 → 있음" 같은 핵심 전제 변경이 한쪽 문서에만 적히고 실제 코드엔 반영 안 된 채
  방치되는 일이 생김 (실제로 이랬다 — 아래 참고)

**새 세션을 시작하면 반드시:**
```bash
cd /Users/gilbert/japantrip
git status --short          # 로컬에 반영 안 된 변경이 있는지
git fetch origin main && git log origin/main -5 --oneline   # 다른 세션/사람이 이미 push했는지
git log -1 --oneline        # 내 로컬 HEAD가 origin과 같은지
```
로컬이 origin보다 뒤처져 있으면(다른 세션이 이미 커밋했으면) **그 커밋들을 먼저 읽고
(`git show <hash>`) 무엇이 바뀌었는지 파악한 다음** 작업을 시작해라. 모르고 덮어쓰면
방금 겪은 것과 같은 혼란이 반복된다. 다른 클론(`Downloads` 등)을 발견하면 거기서
작업하지 말고, 있는 문서만 참고삼아 읽고 `/Users/gilbert/japantrip`으로 가져와라.

---

## 1. 드라이브 확인

- 작업 폴더 ID: `1hDmOPyv3Hnk7hcnMy_3XKngDftg-txwn`
- 새 세션 시작 시, 또는 사용자가 "일정 변동 있어" "문서 추가" 같은 말을 하면:
  ```
  search_files: parentId = '<폴더ID>' and modifiedTime > '<마지막으로 확인한 시각>'
  ```
- **자정 근처 파일을 놓치기 쉽다.** 날짜 필터를 하루 단위(`> '2026-09-21T00:00:00Z'`)로
  느슨하게 잡았다가 그날 밤 늦게(23시대) 올라온 파일을 놓친 적이 있다. 애매하면
  parentId만으로 전체 목록을 한 번 뽑아서 각 파일의 `modifiedTime`을 직접 눈으로 훑어라.
- 파일명 규칙: `-new`, `-new2`, `-new3`… 뒤 숫자가 클수록 최신. 같은 이름이 여러 번
  나타날 수 있다(예: `shopping-new.json`이 빈 스키마로 한 번, 나중에 실제 내용 채워서
  또 한 번 — 파일명이 같아도 `id`와 `modifiedTime`으로 구분해야 한다).
- 지역명이 파일명에 들어간 게 최신 규칙이다: `food-<지역>-new.json`, `food-<지역>-추가-<품목>.json`
  (기존 지역 그룹에 카드 하나만 이어붙이는 용도), `shopping-<주제>-new.json`.

## 2. 드라이브 콘텐츠 가져오기 — 이스케이프 함정

`read_file_content`로 JSON 파일을 가져오면 **원본 그대로가 아니다.** 이 도구는 내용을
"자연어 표현"으로 변환하면서 마크다운 특수문자에 백슬래시를 붙인다:

- `<b>...</b>` → `\<b\>...\</b\>`
- `#B01C1C` (색상 코드) → `\#B01C1C`
- `~` (물결, 숫자 범위) → `\~`
- 이미 JSON 안에서 이스케이프돼 있던 큰따옴표(`\"`)는 **한 번 더** 이스케이프돼서
  `\\\"` (4글자: `\` `\` `\` `"`)로 온다 — 실제 예: 리뷰 인용구 `"~~"라는 후기가` 부분

**그대로 JSON.parse 하면 안 된다.** 받은 내용을 파일에 그대로 옮겨 적고, 다음 정규식으로
언이스케이프한 뒤 파싱해라:

```js
let s = fs.readFileSync("raw.txt","utf8");
s = s.replace(/\\([<>#_~*\[\]()`"\\-])/g, "$1");
const obj = JSON.parse(s);
```

파싱 후 반드시 인용구가 있던 필드 하나를 직접 눈으로 찍어봐서 `\"` 같은 잔여 백슬래시가
없는지 확인해라(`console.log(obj.어딘가.html)`). 처음 몇 번은 이 정규식에 `"`와 `\`를
빼먹어서 인용구 필드에 잔여 백슬래시가 남는 실수를 했다.

## 3. 로컬 데이터에 반영 — surgical하게, 통째로 재직렬화하지 말 것

- `data/itinerary.json`, `data/food.json`, `data/transit.json`, `data/shoppinglist.json`
  각각의 스키마는 `data/SCHEMA.md` 참고.
- **하루 일정 전체 교체(`itinerary.json`의 `days[i]`)**는 `JSON.parse` → 해당 인덱스
  객체 통째로 교체 → `JSON.stringify(data, null, 2)`로 다시 써도 안전하다. 이 파일은
  전체가 이미 완전 펼침 스타일(모든 값이 자기 줄)이라 재직렬화해도 diff가 안 커진다.
- **`food.json`에 새 지역 그룹을 추가하거나 카드 하나를 끼워 넣을 때는 절대
  `JSON.stringify(data, null, 2)`로 전체를 다시 쓰지 마라.** 이 파일은 지역마다
  포맷이 다르다 — 오래된 그룹(난바 등)은 완전 펼침, 나중에 추가된 그룹(고베·나라 등)은
  `{ "t": "..." }` 같은 flat 객체를 한 줄로 압축해서 쓴다. 전체 재직렬화하면 압축돼
  있던 부분이 전부 펼쳐져서 실제 변경(몇 줄)과 무관한 수백 줄짜리 diff가 생긴다.
  **텍스트 삽입(문자열 찾아서 그 앞/뒤에 새 블록 끼워넣기)으로 처리하고, 새로 추가하는
  블록만 기존 압축 스타일에 맞춰 직접 포맷해라.** (이 프로젝트에서 실제로 한 번 이렇게
  전체 재직렬화했다가 `git diff --stat`에 362줄이 찍혀서 `git checkout`으로 되돌리고
  다시 한 적이 있다.)
- 커스텀 압축 포매터가 필요하면: "모든 값이 문자열/불린/null뿐인 flat 객체들로만
  이루어진 배열"은 각 원소를 한 줄로, 그 외는 재귀적으로 펼치는 함수를 짜서 쓴다
  (이전 커밋들에 이 로직이 인라인 node 스크립트로 남아 있다, `git log -p`로 찾을 수 있음).
- 새 지역/카테고리를 추가하면 **`index.html`에도 손대야 한다** (아래 4장).
- 지도 좌표(`MAPDATA`)는 **정확히 아는 것만 추가.** 모르면 지어내지 말고 생략하고
  카드의 `gm`(구글맵 검색어) 링크로 대체해라 — 틀린 좌표보다 없는 게 낫다.

## 4. `index.html` 손대야 하는 경우

- 새 지역(`food.json`에 새 `area`) 추가 시:
  - `#arearow`에 지역 필터 칩 추가
  - `#catrow`에 그 지역 카드들이 쓰는 새 `cat` 값 전부 칩으로 추가 (기존 칩과 겹치는지
    먼저 확인 — `grep -o 'data-c="[^"]*"' index.html | sort -u`)
  - `.group[data-area="..."]` 빈 div 추가 (food 페이지 하단, `<p class="empty">` 앞)
  - 지도는 선택. 없어도 된다 — `공항`/`오사카성`/`신사이바시` 그룹이 전부 지도 없이
    카드만 있는 방식이다. 지도를 만들 거면 `.mapbox` + `lmap-<key>` + `MAPDATA.<key>` +
    `refreshMaps()`와 초기화 배열에 `<key>` 추가까지 다 해야 한다.
- 지역을 완전히 삭제할 때는 반대로: 지도(`lmap-*`, `MAPDATA.*`), 필터 칩(지역+그
  지역 전용이던 종류), `.group[data-area]`, `refreshMaps` 배열에서 전부 제거하고
  **그 지역 카드들만 쓰던 종류 칩도 orphan으로 남지 않게 같이 지운다**
  (`node -e`로 남은 카드들의 cat 목록을 뽑아서 비교하면 빠르다).
- 정적 패널(`#pf` 식당후보, `#ps` 쇼핑 매장가이드, `#pt` 팁·체크리스트)은 JSON이 아니라
  `index.html`에 직접 HTML로 박혀 있다. 여기 흩어진 텍스트가 날짜/장소 언급을 하고
  있으면, 그 날짜/장소가 바뀔 때 같이 깨진다 — 큰 일정 변경 후에는
  `grep -n '<지운 장소명>' index.html`로 정적 패널에 남은 잔해가 없는지 훑어라.

## 5. 빌드 · 검증

```bash
node -e '["data/itinerary.json","data/food.json","data/transit.json","data/shoppinglist.json"].forEach(f=>JSON.parse(require("fs").readFileSync(f,"utf8")));console.log("valid")'
node scripts/build.mjs
tidy -q -e -utf8 index.html 2>&1 | grep -iE "unclosed|missing.*>|discarding|unexpected|not closed" | grep -v "trimming|proprietary|inserting implicit|missing.*doctype|lacks.*doctype" | wc -l
```
마지막 tidy 체크는 **베이스라인이 10**이다(사전 존재하던 구조상 경고, 실제 문제
아님). 이 숫자가 늘어났으면 방금 한 편집이 태그를 깼다는 뜻이다. 한 번 실제로
`<p ... id="empty">` 의 닫는 `>`를 편집 실수로 지운 적이 있다 — grep으로 카운트가
바뀌지 않아도 안심하지 말고, 의심되는 태그는 `grep -c '패턴>'`으로 직접 세어봐라.

`git diff --stat`을 커밋 전에 항상 봐라. 의도한 변경 범위(보통 수십 줄)보다 훨씬 크게
나오면(수백 줄) 3장에서 경고한 "전체 재직렬화" 문제일 확률이 높다 — `git diff <파일>
| grep '^@@'`로 diff가 실제로 손댄 부분에만 몰려 있는지 확인해라.

## 6. HANDOFF.md 기록

바뀐 내용을 `## 2. 일정 구조` 표(요약)와, 맨 아래 이력 섹션(날짜 있는 `###` 항목)
둘 다에 남긴다. 이력 항목엔:
- 무엇을 바꿨는지 (구체적으로 — "9/25 오후 재조정"이 아니라 "디즈니→PANAME→돈키호테로 교체")
- 왜 바뀌었는지 (드라이브 원본 파일명, 사용자 요청 문구 등 근거)
- **삭제되거나 되돌려진 기존 규칙이 있으면 명시.** "설계상 뺀 것 (되돌리지 말 것)" 목록에
  있던 항목이 이번에 다시 살아났으면, 그 목록도 같이 고쳐서 다음 세션이 "실수로
  되살아났다"고 오판해 또 되돌리지 않게 해라 (실제로 "첫날 도톤보리 제외" 규칙이
  이렇게 되돌려진 뒤 목록을 고쳐둔 사례가 있다).
- 확인이 필요하다고 원본에 적혀 있던 내용(예: "현장에서 확인 필요")은 그대로 옮겨 적어라.
  임의로 확정짓지 마라.

## 7. 커밋 · 배포

```bash
git add <바뀐 파일들 이름 하나하나>   # git add -A 지양, .DS_Store 등 딸려 들어갈 수 있음
git commit -m "..."
git push origin main
```
- push 계정은 `eungil` (`gh auth status`로 active account 확인).
- push 직후 GitHub Pages 배포는 **1~2분 걸린다.** `curl`로 라이브 사이트를 바로
  확인했는데 옛날 내용이 보여도 당황하지 말고, 아래로 실제 빌드 상태를 확인해라:
  ```bash
  gh api repos/eungil/japantrip/pages/builds/latest
  ```
  `status`가 `building`이면 정상 진행 중, `built`면 반영 완료. (이전 세션이 이걸
  "배포 파이프라인이 고장났다"고 오판한 적이 있다 — 실제로는 그냥 전파 지연이었다.)

## 8. 그 외 실전에서 겪은 것들

- **이동 시간·환승을 노선 구조만 보고 추정해서 확정처럼 말하지 마라.** 실제로
  나비타임/야후 노선정보로 그 날짜·시각을 넣어 조회한 값인지, 아니면 대충 짐작한
  값인지 항상 구분해서 전달해라. 후자면 "추정치, 현장에서 확인 필요"라고 명시해라.
- **"난바역"은 이 여행 동선에서 최소 4개다** — 난카이 난바역(라피트), 오사카난바역
  (한신·킨테츠), 오사카메트로 난바역(미도스지선), 난바역(요쓰바시선). 어느 회사 노선인지
  같이 안 적으면 나중에 갈아타는 사람이 헷갈린다.
- **인스타그램 링크(`instagram.com/p/...`, `/reel/...`)는 못 연다** (robots.txt 차단,
  우회 불가). 정보가 인스타에 있으면 링크 대신 스크린샷을 요청해라.
- **식당은 특정 가게로 고정하지 않는다** — 사용자가 명시적으로 요청한 패턴이다.
  일정의 식사 스톱은 "가게는 그날 정하세요, 후보는 맛집 지도에" 형식을 유지해라.
- **핵심 전제가 뒤집히면(예: 유모차 없음 → 있음) 전체를 다시 훑어야 한다.** 한
  곳만 고치고 넘어가면 "안고 이동" 류의 문구가 사이트 곳곳(정적 패널 포함)에 낡은
  전제로 남는다. `grep -rn '안고\|유모차'` 로 전체를 훑고 하나씩 판단해라.
- **좌표를 모르면 직선거리로 도보시간을 추정하지 마라** — 실제 도로망과 다르다.
  참고용이라고 명시하거나 아예 생략해라.
