# 오사카 가족여행 사이트 — 프로젝트 컨텍스트

## 정체
- 2026년 9월 23~26일 오사카 가족여행(어른 2 + 아이 36·70개월) 일정 사이트
- 결과물은 **정적 HTML 한 장** (`index.html`). 빌드 없음. 프레임워크 없음
- 배포: GitHub Pages, `main` 브랜치 루트 → https://eungil.github.io/japantrip/
- 저장소 소유자 계정은 `eungil` (github.com). 푸시 시 이 계정 사용

## 작업 전 필수
`HANDOFF.md`를 먼저 읽는다. 거기에:
- 여행 확정 사실관계와 아이 동반 제약(유모차·아기띠 없음 등)
- 4일 일정 구조와 "설계상 뺀 것 — 되돌리지 말 것" 목록
- 결과물 HTML 구조 (페이지 2개, sticky 레이어, CSS 네임스페이스)
- 시간을 잡아먹은 버그 5건의 원인과 해법
- 장소 데이터(MAPDATA) 형식과 요일별 휴무
- 서술 톤 원칙

## 코드 규칙 (HANDOFF 3·4장 요약)
- 새 CSS는 반드시 `#page-food` 또는 일정 쪽 스코프를 명시. 두 페이지가 원래 별도 문서라 클래스명이 겹침
- **주석 문자열을 앵커로 삼아 코드를 치환하지 말 것.** 같은 주석이 CSS와 JS 양쪽에 있어 사고가 났음. 삽입 후 `</style>` 위치 < `var MAPDATA` 위치인지 검증
- Leaflet CDN 태그에 `integrity`(SRI) 넣지 말 것 — 해시 어긋나면 조용히 차단됨
- 숨겨진(`display:none`) 컨테이너에서 `L.map()` 초기화 금지 → 크기 0. 전환·load·resize에서 `invalidateSize()` 재호출
- SVG 지도로 되돌리지 말 것 (숨겨진 부모 안에서 높이 0 계산됨). 개념도(`.pmap`)는 2026-09-10 제거됨 — 되살리지 말 것
- `.pageswitch` 높이(52px) 바꾸면 `.tabs`·`.filters`의 `top:52px`도 같이 바꿀 것

## 변경 플로우
1. `index.html` 수정
2. 로컬에서 브라우저로 열어 눈으로 확인 (일정 페이지 + 맛집 지도 두 페이지 모두)
3. 커밋·푸시 → 1~2분 뒤 Pages 반영 확인

## 서술 톤
존댓말 담백체. "왜 그렇게 짰는지"를 항상 함께 적는다. 아이 동반 관점의 실질적 경고 우선.
가게마다 아이 동반 난이도 ◎○△✕. 영업시간·요금은 재확인 필요하다고 명시.
한자 쓰지 말 것 (장소 고유명사의 일본어 표기는 예외).
